# Superseded baseline diagnostic

This initial run forced HWP on remainder batches. It is retained as a diagnostic, not final improvement evidence. The final run permits direct fallback per baseline batch. Artifact paths inside the initial JSON refer to the original results directory; the corresponding immutable files are preserved here by basename.
